#include<stdio.h>
int main()
{
    int integerType;
    float floatType;
    char charType;
    printf("\nsize of int is=%d",sizeof(int));
    printf("\nsize of float is=%d",sizeof(float));
    printf("\nsize of char is=%d",sizeof(char));
    return 0;
}